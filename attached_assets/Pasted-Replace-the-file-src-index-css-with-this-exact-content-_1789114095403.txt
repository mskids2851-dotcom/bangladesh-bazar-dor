Replace the file src/index.css with this exact content, do not modify anything:

FILE: src/index.css
:root {
  --bg: #FBF5EA;
  --surface: #FFFFFF;
  --ink: #201F1D;
  --ink-soft: #6B6558;
  --teal: #1B3A34;
  --teal-light: #2C544C;
  --mango: #E8833A;
  --mango-dark: #C96A28;
  --line: #EAE0CE;
  --danger: #B4453B;
  --radius: 16px;
}

* { box-sizing: border-box; }
html, body, #root { height: 100%; }

body {
  margin: 0;
  background: var(--bg);
  color: var(--ink);
  font-family: "Noto Sans Bengali", "Hind Siliguri", -apple-system, sans-serif;
}

.app-shell {
  max-width: 480px;
  margin: 0 auto;
  min-height: 100vh;
  background: var(--bg);
  display: flex;
  flex-direction: column;
}

.topbar {
  background: var(--teal);
  background-image: repeating-linear-gradient(
    -45deg,
    rgba(232, 131, 58, 0.10) 0px,
    rgba(232, 131, 58, 0.10) 10px,
    transparent 10px,
    transparent 20px
  );
  color: #fff;
  padding: 20px 20px 18px;
  position: sticky;
  top: 0;
  z-index: 5;
}

.topbar h1 {
  margin: 0;
  font-size: 20px;
  font-weight: 700;
  letter-spacing: 0.2px;
}

.topbar .sub {
  margin-top: 3px;
  font-size: 12.5px;
  color: #C9D9CE;
}

.content { flex: 1; padding: 18px 16px 96px; }

.card {
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  padding: 16px 18px;
  margin-bottom: 14px;
  box-shadow: 0 1px 2px rgba(27, 58, 52, 0.04);
}

.cat-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
  margin-top: 16px;
}

.cat-tile {
  background: var(--surface);
  border: 1px solid var(--line);
  border-left: 4px solid var(--mango);
  border-radius: var(--radius);
  padding: 20px 16px;
  text-align: left;
  font-size: 15.5px;
  font-weight: 600;
  color: var(--ink);
}

.cat-tile:nth-child(2n) { border-left-color: var(--teal-light); }
.cat-tile:nth-child(3n) { border-left-color: #A6742F; }
.cat-tile:active { background: #F5EFDF; }

label {
  display: block;
  font-size: 13px;
  color: var(--ink-soft);
  margin: 14px 0 6px;
  font-weight: 600;
}

input, select, textarea {
  width: 100%;
  padding: 12px 13px;
  border-radius: 11px;
  border: 1px solid var(--line);
  background: #fff;
  font-size: 15px;
  color: var(--ink);
  font-family: inherit;
}

input:focus, select:focus, textarea:focus {
  outline: none;
  border-color: var(--mango);
}

button.primary {
  width: 100%;
  margin-top: 20px;
  padding: 14px;
  border: none;
  border-radius: 11px;
  background: var(--mango);
  color: #fff;
  font-size: 15.5px;
  font-weight: 700;
}
button.primary:active { background: var(--mango-dark); }
button.primary:disabled { background: #E9C6A6; }

button.ghost {
  width: 100%;
  padding: 13px;
  border-radius: 11px;
  border: 1px solid var(--line);
  background: transparent;
  color: var(--ink-soft);
  font-size: 14px;
  margin-top: 9px;
  font-weight: 600;
}

button.link {
  background: none;
  border: none;
  color: var(--teal-light);
  font-size: 14px;
  padding: 7px 0;
  font-weight: 600;
}

.auth-tabs {
  display: flex;
  background: #F1EAD8;
  border-radius: 11px;
  padding: 4px;
  margin-bottom: 6px;
}
.auth-tabs button {
  flex: 1;
  border: none;
  background: transparent;
  padding: 10px 0;
  border-radius: 8px;
  font-size: 14px;
  font-weight: 600;
  color: var(--ink-soft);
}
.auth-tabs button.active {
  background: #fff;
  color: var(--teal);
  box-shadow: 0 1px 2px rgba(0,0,0,0.08);
}

.price-row {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 13px 0;
  border-bottom: 1px solid var(--line);
}
.price-row:last-child { border-bottom: none; }
.price-row .name { font-weight: 700; font-size: 15px; }
.price-row .meta { font-size: 12.5px; color: var(--ink-soft); margin-top: 3px; }
.price-row .amount {
  font-weight: 700;
  color: #fff;
  background: var(--teal);
  padding: 4px 11px;
  border-radius: 20px;
  font-size: 14px;
  white-space: nowrap;
}

.badge {
  display: inline-block;
  font-size: 11px;
  padding: 2px 9px;
  border-radius: 20px;
  margin-left: 6px;
  font-weight: 600;
}
.badge.pending { background: #FBEBD1; color: #8A5A17; }
.badge.approved { background: #DCEEDF; color: var(--teal); }
.badge.rejected { background: #F6DCD9; color: var(--danger); }

.navbar {
  position: fixed;
  bottom: 12px;
  left: 50%;
  transform: translateX(-50%);
  width: calc(100% - 24px);
  max-width: 456px;
  background: #fff;
  border: 1px solid var(--line);
  border-radius: 18px;
  display: flex;
  padding: 8px 6px;
  box-shadow: 0 6px 18px rgba(27, 58, 52, 0.12);
}

.navbar button {
  flex: 1;
  background: none;
  border: none;
  font-size: 11.5px;
  color: var(--ink-soft);
  padding: 7px 0;
  border-radius: 12px;
  font-weight: 600;
}
.navbar button.active { color: #fff; background: var(--teal); }

.error-text { color: var(--danger); font-size: 13px; margin-top: 9px; }
.success-text { color: var(--teal); font-size: 13px; margin-top: 9px; font-weight: 600; }
.empty { text-align: center; color: var(--ink-soft); padding: 34px 10px; font-size: 14px; }
.top-back { background: none; border: none; color: #C9D9CE; font-size: 14px; padding: 0; margin-bottom: 6px; font-weight: 600; }

After creating this exactly as given, restart the app and tell me the result.